
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-ui
app:
  firebase:
    credential:
      path: ${APP_FIREBASE_CREDENTIAL_PATH}
      